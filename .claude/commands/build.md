Build Ingnomia and report the results.

1. Use the `build_game` MCP tool from `ingnomia-test` to build
2. If the build fails, analyze the compiler errors and suggest fixes
3. If the build succeeds with warnings, list notable warnings
4. If the build is clean, just confirm success
